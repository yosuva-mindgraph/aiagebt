config.js
node_modules/
shots/
dist/stand.html
# dist/index.html is committed on purpose — it is the keyless file you hand out.
# dist/stand.html is the same file with config.js baked in — never commit it.
